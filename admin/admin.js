import './admin.sass';

function onReady() {
  // script that runs when everything is ready
}

function onLoad() {
  // script that runs when everything is loaded
}

document.addEventListener('DOMContentLoaded', onReady);
window.addEventListener('load', onLoad);